import {} from "../types.js"

const initialState = {}


const recipeReducer = (state=initialState, action) =>{
    switch(action.type){
        default:
            return state
    }
}

export default recipeReducer